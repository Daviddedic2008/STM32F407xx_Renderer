#ifndef MODEL
#define MODEL

void loadCube(const uint16_t color);     // 12 tris
void loadPyramid(const uint16_t color);  // 6 tris
void loadColumn(const uint16_t color);   // 12 tris
void loadGround(const uint16_t color);


#endif
